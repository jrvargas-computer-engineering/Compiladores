multiplica_decimal -> decimal com a := decimal, b := decimal :=
[
    retorna a * b := decimal
],

principal -> inteiro :=
[
    var d := decimal
    d := multiplica_decimal(3, 2)
    retorna 0 := inteiro
];
